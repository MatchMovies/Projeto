/*
package com.br.matchmovies.viewmodel

import androidx.lifecycle.MutableLiveData
import androidx.lifecycle.ViewModel

class MatchsViewModel : ViewModel() {

    val matchsLiveData: MutableLiveData<MutableList<ListMatchs>> by lazy { MutableLiveData<MutableList<ListMatchs>>() }

    init {
        matchsLiveData.postValue(getMatchList())
    }

    private fun getMatchsListFilmes(): MutableList<ListMatchs> {
        val listMatchsListFilmes = mutableListOf<ListMatchs>()
                listMatchsListFilmes.add(ListMatchs("Filmes", getMatchsList())

                        return listMatchsListSeriados
    }

    private fun getMatchsListSeriados(): MutableList<ListMatchs> {
                    val listMatchsListSeriados = mutableListOf<ListMatchs>()
                listMatchsListSeriados.add(ListMatchs("Seriados", getMatchsList())

                return listMatchsListSeriados
    }
}
*/
